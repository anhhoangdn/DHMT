name: deca_dhmt
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.8
  - pip:
      - pip                        # ← thêm dòng này (tránh warning)
      - mediapipe>=0.10.0
      - opencv-python>=4.8.0
      - numpy>=1.24.0
      - PyYAML>=6.0
      - Pillow>=10.0.0
      - tqdm>=4.65.0
      - scipy==1.10.1              # ← đổi: bản cuối hỗ trợ Python 3.8
      - matplotlib>=3.7.0