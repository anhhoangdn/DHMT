name: deca_dhmt
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
    - mediapipe>=0.10.0
    - opencv-python>=4.8.0
    - numpy>=1.24.0
    - PyYAML>=6.0
    - Pillow>=10.0.0
    - tqdm>=4.65.0
    - scipy>=1.11.0
    - matplotlib>=3.7.0
    # DECA dependencies installed separately:
    # pip install -r external/DECA/requirements.txt
